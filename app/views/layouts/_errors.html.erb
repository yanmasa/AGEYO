<% if obj.errors.any? %>
  <div id="error_explanation">
    <p><%= pluralize(obj.errors.count, "error") %> prohibited this obj from being saved:</p>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
